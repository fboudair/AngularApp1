export interface Reservation{ //this file create an interface that represent the rreservation
    id?: number;
    areaId: number;
    name: string;
    email: string;
    date: string;
    time: string;
}